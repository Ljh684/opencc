// Generated by tools/gen_dict from data/opencc/config/*.json — do not edit by hand.
//
// `passes` is the normalisation pass followed by the conversion chain, in the
// order OpenCC applies them. `missing` lists dictionaries the configuration
// references but the pinned snapshot does not ship (OpenCC produces them during
// its own build); the runtime builder skips those leaves and reports them.

///|
pub(all) enum Policy {
  Union
  ShortCircuit
}

///|
pub(all) enum Spec {
  Leaf(String)
  Group(Policy, Array[Spec])
}

///|
pub struct ChainSpec {
  name : String
  title : String
  missing : Array[String]
  normalization : Array[Spec]
  segmentation : Spec?
  conversion : Array[Spec]
}

///| Every configuration of the pinned snapshot.
pub let chains : Array[ChainSpec] = [
  {
    name: "hk2s",
    title: "Traditional Chinese (Hong Kong variant) to Simplified Chinese",
    missing: ["hk_variants_rev", "ts_characters_ext", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Leaf("ts_phrases")),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("hk_variants_rev_phrases"), Spec::Leaf("hk_variants_rev"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("ts_phrases"), Spec::Leaf("ts_characters_ext"), Spec::Leaf("ts_characters"), ]),
    ],
  },
  {
    name: "hk2sp",
    title: "Traditional Chinese (Hong Kong variant) to Simplified Chinese (with phrases)",
    missing: ["hk_variants_rev", "ts_characters_ext", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Leaf("ts_phrases")),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("hk_phrases_rev"), Spec::Leaf("hk_variants_rev_phrases"), Spec::Leaf("hk_variants_rev"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("ts_phrases"), Spec::Leaf("ts_characters_ext"), Spec::Leaf("ts_characters"), ]),
    ],
  },
  {
    name: "hk2t",
    title: "Traditional Chinese (Hong Kong variant) to Traditional Chinese (OpenCC Standard)",
    missing: ["hk_variants_rev", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("hk_variants_rev_phrases"), Spec::Leaf("hk_variants_rev"), ]),
    ],
  },
  {
    name: "jp2t",
    title: "New Japanese Kanji (Shinjitai) to Old Japanese Kanji (Kyūjitai)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("jp_shinjitai_phrases"), Spec::Leaf("jp_shinjitai_characters"), ]),
    ],
  },
  {
    name: "s2hk",
    title: "Simplified Chinese to Traditional Chinese (Hong Kong variant)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ])),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ]), Spec::Leaf("st_characters"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("hk_variants_phrases"), Spec::Leaf("hk_variants"), ]),
    ],
  },
  {
    name: "s2hkp",
    title: "Simplified Chinese to Traditional Chinese (Hong Kong variant, with phrases)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ])),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ]), Spec::Leaf("st_characters"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("hk_phrases"), Spec::Leaf("hk_variants_phrases"), Spec::Leaf("hk_variants"), ]),
    ],
  },
  {
    name: "s2seal",
    title: "Simplified Chinese to Small Seal Script",
    missing: ["seal_characters_rev", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ]), Spec::Leaf("st_characters"), ]),
      Spec::Leaf("seal_variants"),
      Spec::Leaf("seal_characters_rev"),
    ],
  },
  {
    name: "s2t",
    title: "Simplified Chinese to Traditional Chinese (OpenCC Standard)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ]), Spec::Leaf("st_characters"), ]),
    ],
  },
  {
    name: "s2tw",
    title: "Simplified Chinese to Traditional Chinese (Taiwan Standard)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ])),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ]), Spec::Leaf("st_characters"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("tw_variants_phrases"), Spec::Leaf("tw_variants"), ]),
    ],
  },
  {
    name: "s2twp",
    title: "Simplified Chinese to Traditional Chinese (Taiwan Standard, with Taiwan Phrases)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ])),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Group(Policy::Union, [Spec::Leaf("st_phrases"), Spec::Leaf("st_phrases_generated_from_regional_phrases"), ]), Spec::Leaf("st_characters"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("tw_phrases"), Spec::Leaf("tw_variants_phrases"), Spec::Leaf("tw_variants"), ]),
    ],
  },
  {
    name: "seal2t",
    title: "Small Seal Script to Traditional Chinese Characters",
    missing: ["seal_variants_rev", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Leaf("seal_characters"),
      Spec::Leaf("seal_variants_rev"),
    ],
  },
  {
    name: "t2hk",
    title: "Traditional Chinese (OpenCC Standard) to Traditional Chinese (Hong Kong variant)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("hk_variants_phrases"), Spec::Leaf("hk_variants"), ]),
    ],
  },
  {
    name: "t2jp",
    title: "Old Japanese Kanji (Kyūjitai) to New Japanese Kanji (Shinjitai)",
    missing: ["jp_shinjitai_characters_rev", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Leaf("jp_shinjitai_characters_rev"),
    ],
  },
  {
    name: "t2s",
    title: "Traditional Chinese (OpenCC Standard) to Simplified Chinese",
    missing: ["ts_characters_ext", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("ts_phrases"), Spec::Leaf("ts_characters_ext"), Spec::Leaf("ts_characters"), ]),
    ],
  },
  {
    name: "t2seal",
    title: "Traditional Chinese Characters to Small Seal Script",
    missing: ["seal_characters_rev", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Leaf("seal_variants"),
      Spec::Leaf("seal_characters_rev"),
    ],
  },
  {
    name: "t2tw",
    title: "Traditional Chinese (OpenCC Standard) to Traditional Chinese (Taiwan Standard)",
    missing: [],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("tw_variants_phrases"), Spec::Leaf("tw_variants"), ]),
    ],
  },
  {
    name: "tw2s",
    title: "Traditional Chinese (Taiwan Standard) to Simplified Chinese",
    missing: ["tw_variants_rev", "ts_characters_ext", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Leaf("ts_phrases")),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("tw_variants_rev_phrases"), Spec::Leaf("tw_variants_rev"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("ts_phrases"), Spec::Leaf("ts_characters_ext"), Spec::Leaf("ts_characters"), ]),
    ],
  },
  {
    name: "tw2sp",
    title: "Traditional Chinese (Taiwan Standard) to Simplified Chinese (Mainland China Phrases)",
    missing: ["tw_variants_rev", "ts_characters_ext", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: Some(Spec::Leaf("ts_phrases")),
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("tw_phrases_rev"), Spec::Leaf("tw_variants_rev_phrases"), Spec::Leaf("tw_variants_rev"), ]),
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("ts_phrases"), Spec::Leaf("ts_characters_ext"), Spec::Leaf("ts_characters"), ]),
    ],
  },
  {
    name: "tw2t",
    title: "Traditional Chinese (Taiwan Standard) to Traditional Chinese (OpenCC Standard)",
    missing: ["tw_variants_rev", ],
    normalization: [
      Spec::Leaf("cjk_compatibility_ideographs"),
    ],
    segmentation: None,
    conversion: [
      Spec::Group(Policy::ShortCircuit, [Spec::Leaf("tw_variants_rev_phrases"), Spec::Leaf("tw_variants_rev"), ]),
    ],
  },
]
